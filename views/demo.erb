<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Inflect Demo</title>

  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
  <link rel="stylesheet" href="assets/demo.css">
</head>
<body>

  <div class="container">
    <div class="row">
      <div class="col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4 col-lg-4 col-lg-offset-4">
        <form method="POST" action="/handle">
          <h2 >Inflect</h2>
          <label for="word" class="sr-only">Palabra</label>
          <input name="word" type="text" class="form-control" placeholder="Word for search..." required autofocus>
          <br>
          <button class="btn btn-lg btn-primary btn-block" type="submit">Buscar</button>
        </form>
        <% if defined? inflection %>
          <% if list? inflection %>
            <h3><%= inflection.content[:title] %></h3>

            <ul class="list-group">
              <% inflection.content[:body].each do | item | %>
              <li class="list-group-item">
                <% if item.is_a? Array %>
                  <%= "#{item.shift}: #{item.shift}" %>
                <% else %>
                  <%= item %>
                <% end %>
              </li>
              <% end %>
            </ul>
          <% else %>
            <h3><%= inflection.content %>.</h3>
          <% end %>
        <% end %>

      </div>
    </div>
  </div>

  <!-- Latest compiled and minified JavaScript -->
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js" integrity="sha384-0mSbJDEHialfmuBBQP6A4Qrprq5OVfW37PRR3j5ELqxss1yVqOtnepnHVP9aJ7xS" crossorigin="anonymous"></script>
</body>
</html>
